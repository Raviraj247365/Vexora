[tool.poetry]
name = "vexora-ai"
version = "0.1.0"
description = "AI services for Vexora (placeholder)"

[tool.poetry.dependencies]
python = ">=3.10,<3.12"

[tool.poetry.dev-dependencies]
pytest = "^7.0"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
