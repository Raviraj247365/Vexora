Backend (Node.js)
------------------

Purpose:
- REST / GraphQL API, authentication, file upload orchestration.

Folder role:
- `src/` application source
- `tests/` automated tests
- `Dockerfile` container image instructions

Notes:
- Use `node` and `npm` (or `yarn`) to manage packages.
- Store runtime secrets in environment variables and secrets manager.
