Shared Libraries
----------------

Purpose:
- Host shared code (TypeScript types, Dart packages, client SDKs) used across apps and services.

Folder role:
- `ts/` for TypeScript shared clients
- `dart/` for Dart shared packages

Notes:
- Keep this code well-documented and versioned to avoid coupling issues.
