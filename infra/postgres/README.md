PostgreSQL Infra
-----------------

Purpose:
- Database setup and initialization helper scripts.

Folder role:
- `docker-init.sql` sample schema bootstrap
- `Dockerfile` or docker-compose fragments to run Postgres in dev

Notes:
- Use managed Postgres in production; keep local scripts for development only.
